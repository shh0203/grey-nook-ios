import Flutter
import UIKit
import XCTest

class RunnerTests: XCTestCase {
  func testExample() {
    // If you add a Flutter-specific test, you can use the following
    // helper to make it easier to look up widgets in the widget tree.
    let _: FlutterEngine = FlutterEngine(name: "test_engine")
  }
}
